# Start lenient; raise the profile (moderate -> safety -> shared -> production)
# as the playbooks are cleaned up. See https://ansible.readthedocs.io/projects/lint/profiles/
profile: basic

exclude_paths:
  - .github/
